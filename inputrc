# This file is read by Readline programs such as Bash; it does not affect Zsh.

# Show all completion matches immediately when completion is ambiguous.
set show-all-if-ambiguous on

# Ignore uppercase/lowercase differences during tab completion.
# Example: "doc<TAB>" can match "Documents".
set completion-ignore-case on

# Append "/" to completed directory names.
set mark-directories on

# Also append "/" for symlinked directories.
set mark-symlinked-directories on

# Do not include hidden files/directories (dotfiles) in tab completion
# unless explicitly requested by typing "." first.
set match-hidden-files off

# Show file type indicators in completions:
#   / = directory
#   * = executable
#   @ = symlink
set visible-stats on

# Keep interactive shells quiet when no completion or history match exists.
set bell-style none

# Search history using the command prefix already typed on the current line.
"\e[A": history-search-backward
"\e[B": history-search-forward
"\eOA": history-search-backward
"\eOB": history-search-forward
